/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://b3devs.com', // change to your live domain
  generateRobotsTxt: true,       // will create robots.txt too
  sitemapSize: 7000,
};
